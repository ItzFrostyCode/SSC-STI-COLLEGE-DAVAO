{
  "name": "ssc-sti-davao",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public && cp *.html public/ && cp -r assets public/ && cp -r src public/ && cp -r data public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
